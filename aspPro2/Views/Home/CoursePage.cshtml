@model List<aspPro2.Models.Course>
@{
    ViewData["Title"] = "Home Page";
    Layout = "~/Views/Shared/_NewLayout1.cshtml";
}
<head>
    <link href="~/lib/bootstrap/dist/css/CRUDStyle.css" rel="stylesheet" />
</head>

<div class="crud-root">

    <div class="crud-header">
        <div class="crud-header-left">
            <div class="crud-eyebrow">Academic Management</div>
            <h1>________________</h1>
        </div>
        
    </div>

    <div class="table-wrap">
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Title</th>
                    <th>Department</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var course in Model)
                {
                    <tr>
                        <td class="td-id">#@course.Id</td>
                        <td class="td-title">@course.Title</td>
                        <td>
                            <span class="td-dept">
                                <span class="dept-dot"></span>
                                @course.DepartmentId
                            </span>
                        </td>
                        <td>
                            <div class="actions-cell">
                                <button class="edit-btn"
                                        onclick="location.href='@Url.Action("EditCourse", "Home", new { id = course.Id })'">
                                    Edit
                                </button>
                                <form asp-action="DeleteCourse" asp-controller="Home" method="post">
                                    <input type="hidden" name="id" value="@course.Id" />
                                    <button type="submit" class="delete-btn">Delete</button>
                                </form>
                                
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <div class="crud-footer">
        <a class="btn-add" href="@Url.Action("CreateCourse", "Home")">
            <span class="btn-add-icon">+</span>
            Add New Course
        </a>
        <span class="crud-footer-text">@Model.Count() course(s) total</span>
        <a class="back-link" href="@Url.Action("Index", "Home")">
            ← Back to Dashboard
        </a>
    </div>

</div>